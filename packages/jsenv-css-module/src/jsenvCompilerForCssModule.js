/*

le truc c'est que ce compiler peut fonctionner
(on utilise postCSS pour retourner un autre css)
mais par contre impossible d'écrire
import style from 'style.css'
et d'obtenir du JSON

pour avoir ça il faudrait qu'on retourne le JSON et non le CSS
mais que le CSS soit "magiquement" inséré dans le DOM non ?
*/

export const jsenvCompilerForCssModule = () => {}
